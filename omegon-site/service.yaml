apiVersion: v1
kind: Service
metadata:
  name: omegon-site
  namespace: omegon-site
  labels:
    app.kubernetes.io/name: omegon-site
spec:
  selector:
    app.kubernetes.io/name: omegon-site
  ports:
    - name: http
      port: 80
      targetPort: http
