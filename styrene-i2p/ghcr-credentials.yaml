# Sealed secret — ghcr-secret for pulling images from ghcr.io/styrene-lab
# Apply the same ghcr-secret used in styrene-docs namespace
apiVersion: v1
kind: Secret
metadata:
  name: ghcr-secret
  namespace: styrene-i2p
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: PLACEHOLDER
